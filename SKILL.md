---
name: hotel-assistant
slug: hotel-assistant
displayName: 酒店助手
description: 酒店比价助手——同酒店同房型同政策，比总价。当用户想比酒店价格、酒店哪个平台便宜、订酒店哪里划算、住宿比价、含早对比、取消政策、hotel price comparison 时使用。覆盖：同程旅行、飞猪、美团酒店、携程、去哪儿。按日期+城市+星级筛选，多房型同屏对比，含早/取消政策差异可视化标注，促销价与连住优惠提醒。需入住日期与城市信息；平台网页端需登录/定位时移交用户处理。只做比价，不代下单、不代付款。
summary: "酒店比价助手——同酒店同房型同政策，比总价。当用户想比酒店价格、酒店哪个平台便宜、订酒店哪里划算、住宿比价、含早对比、取…"
version: 1.0.0
license: MIT
tags: 酒店比价,订酒店,住宿,携程,美团酒店,飞猪,同程,去哪儿,含早,取消政策,连住优惠,hotel-comparison,酒店助手
---

# 酒店助手

同一家酒店、同一房型、同一入住政策，比**总价**，不比"看起来便宜的标价"。

## 工作流

1. **解析需求**：提取 `城市`、`入住日期`、`离店日期`、`人数`、可选 `酒店名/位置`、`星级/品牌偏好`、`房型`、`预算`。缺城市或日期时先问一次——酒店价格随日期波动极大，无日期比价无意义。

2. **平台路由**：默认同程旅行 → 飞猪 → 美团酒店 → 携程 → 去哪儿（用户点名平台时以用户为准）。动手前读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")` 操作浏览器。

3. **逐平台采集**：打开入口 → 输入城市/日期 → 搜索 → 筛选酒店 → 打开酒店详情页读房型价。每条记录至少含：`平台`、`酒店`、`房型`、`含早/不含早`、`每晚价`、`入住总价`、`取消政策`、`会员价/促销价（如有）`、`链接`。

4. **登录/定位移交**：登录墙、验证码、定位墙出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户扫码/授权后交回再重读。用户放弃某平台时标注"未采集"。

5. **口径归一（读 `references/hotel-assistant-knowledge.md`）**：
   - **比总价不比每晚价**：多晚连住时总价含税费/服务费，才是真实成本；
   - **同日同房型**：只比同入住日期的同一房型；含早与不含早分开列，不直接比较；
   - **取消政策分级**：免费取消 / 限时免费取消 / 不可取消，政策不同价格不可直接比；
   - **会员价口径**：会员价依赖登录态，页面实显才计入并注明。

6. **报告输出**：Markdown 表 `平台 | 酒店 | 房型 | 含早 | 每晚价 | 总价 | 取消政策 | 链接 | 备注`，结论给出"最低总价渠道 + 政策差异提醒"。**每个价格必须附可点击的预订链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为内部实现（复用主技能「万物比价」的 `data/union_config.json` 与 `scripts/link_helper.py`，对使用者透明）。

7. **盯价提醒**（可选）：用户说"这家酒店降到 X 提醒我"时，写入盯价清单并定时检查（读 `doubao-cron-scheduler/SKILL.md`）。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单酒店 ≤2 次/日、同一酒店只核 1 个详情页、两次页面操作间隔 ≥10 秒、导航后停留 3~8 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证/定位走 `interaction.request_action` 移交。
- 不导出 Cookie/密码/登录态；不代下单、不代付款。
- 价格以页面实读为准，禁止编造；失败平台列原因。

## 边界声明

- 酒店价格随库存实时变动，报告标注"价格截至 <读取时点>"，以平台实时显示为准。
- 民宿/公寓等非标住宿（聚推客民宿频道）与酒店口径不同（清洁费、退订政策差异大），分开列示。
- 取消政策、押金、到店付等条款以酒店页面为准，报告提示"下单前二次确认"。
